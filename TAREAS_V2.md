# TAREAS v2 — lista viva

Reemplaza a `TAREAS.md` como lista viva desde el 2026-09-06; aquel archivo queda como historial cerrado de las fases 0–5 y de la cadena de PRs #6–#12. Aquí solo entran pendientes: marcar con `[x]` al completar y retirar lo cerrado al publicar el cierre.

Las decisiones de producto, interfaz y operación consolidadas el 2026-09-10 están en `docs/AGENDA_PRODUCTO_UI_2026-09-10.md`; usar ese archivo para distinguir lo aprobado, lo descartado y lo que aún requiere definición. El borrador de reglas de condición, permiso y resultado está en `docs/FLUJOS_CONDICIONALES_SISTEMA_2026-09-10.md`.

Orden deliberado: primero limpieza y eliminación de residuos, después el backlog de producto y operación.

## 1. Limpieza de interfaz y residuos (prioridad)

### Ronda de limpieza de interfaz pedida el 2026-09-06

Cerrada el 2026-09-06 en la rama `feat/limpieza-interfaz` (commits `46757b8` y `5a30116`, capturas en `capturas/2026-09-06_limpieza-ui/`). Además de lo pedido: se retiró el CSS muerto que dejó la ronda (`.salon-odoo__resumen`, `.pos-nav__areas`, `.salon-odoo__eyebrow`), se alineó el panel de menú a la izquierda (la utilidad `justify-center` ganaba por capas), se llevaron los objetivos táctiles del salón y del toggle a 44 px y se borraron los duplicados " 2" del árbol.

- [x] Un solo botón de menú: unificar el icono de menú y el de usuario (hoy solo sirve para cerrar sesión) en un único control.
- [x] Eliminar la barra "Sección actual" del encabezado de la página.
- [x] Unificar mesero/cocina dentro de Órdenes: quitar el cambio de vista global de la pantalla; la sección Órdenes tendrá dos vistas conmutables (toggle, los iconos actuales o ambos): con cocina muestra la tabla que hoy ve el cocinero y con mesero la lista que hoy muestra Órdenes. La vista cocina pasa a llamarse "Órdenes", conservando el diseño actual de cada vista salvo el título.
- [x] Salón: el resumen (libres / en servicio / precuenta / atrasadas) en una misma línea, pudiendo mostrar solo iconos sin texto.
- [x] Encabezado del salón lineal y compacto: el título y el botón "+" de nueva orden no pueden quedar apilados ni ocupar un área tan grande; todo en línea en monitor, tablet y smartphone.
- [x] Pantallas grandes: en monitor el salón deja mucho espacio en blanco; que las mesas y botones se adapten al ancho disponible y se minimicen los huecos innecesarios, apoyándose en las skills de diseño (`frontend-design`, `modern-web-guidance`). La grilla de teléfono gusta como está hoy: no tocarla.

### Residuos de color y código muerto

Cerrados el 2026-09-07 en la misma rama `feat/limpieza-interfaz` (commits `440ff68` y `42a2e9a`, capturas en `capturas/2026-09-07_residuos-ui/`, PR #14 ampliado). Detalle del cierre:

- [x] Púrpura fuera de marca: `COLOR_INICIAL` de `CrearProducto.tsx` pasó al cobre de marca `#8a4a26` (cerrado con la ronda del 2026-09-06).
- [x] ~96 hex sueltos en pantallas viejas: migrarlos a las variables de marca/tokens existentes. Quedaron 0 hex fuera de `:root` en `styles.css` (tokens, `color-mix` para bordes suaves o declaraciones muertas borradas); un token nuevo justificado: `--paper` (crema de tickets).
- [x] `switch.tsx` para Opciones (hoy hay un control ad hoc). Patrón de la casa sin Radix; 12 interruptores migrados; fuera el CSS `switch-tablet`.
- [x] Pantallas de tabla del §5 de `MIGRACION_SHADCN` sin migrar a JSX. Resultó desactualizado: todas ya usan shadcn; el trabajo real era consolidar las familias CSS apiladas (Backend, Categorías, page-header quedaron en una sola generación). Quedan pendientes las pantallas grandes de la tabla §5.
- [x] Pseudo-fotos de la carta: el seed ya no las guarda y cada arranque compara el payload legacy exacto (fotos reales intactas); "Extra" muestra su icono de categoría.

## 2. Backlog de producto y operación

- [x] Recuperar la verificación completa del entorno local (2026-09-12). Cerrado en la copia local fuera de iCloud después de recuperar todos los archivos. `npm ci` reinstaló 171 paquetes desde el lockfile. `npm test`: 81 archivos y 494 pruebas aprobadas en 41,20 s, usando `happy-dom`, aislamiento original y sin excluir pruebas ni ampliar tiempos. `npm run test:go`: los 15 paquetes con pruebas aprobados; `npm run build`: tipos de código y pruebas `.ts/.tsx` más producción React aprobados (Vite: 19,48 s); `npm run build:go` y `npm run licenses` aprobados (24 dependencias directas). El binario se comprobó en `127.0.0.1:18091` con datos temporales aislados: salud Go/SQLite, sesión cerrada, API protegida, HTML y bundle React correctos. El proceso temporal se detuvo; no se abrió ni modificó la base operativa. No equivale a una auditoría visual o de producto completa.
- [x] Separar el desarrollo de iCloud (2026-09-12). Copia completa en `/Users/luisgonzalez/Developer/Restaurante`, conservando el original `/Users/luisgonzalez/Documents/Restaurante`. Antes de reinstalar dependencias, la comparación recursiva de contenidos fue idéntica y Git conservó los mismos 161 archivos modificados o nuevos y la rama `codex/propuesta-mesas`, sin crear commits ni publicar cambios. La copia no tiene archivos `dataless` ni pertenencia al dominio File Provider de iCloud. Lanzador y rutas revisados; la BD sigue fuera del repositorio por defecto. README actualizado en la copia local con ubicación de desarrollo, traslado seguro y separación de respaldos. Continuar el desarrollo únicamente en `Developer/Restaurante`; el original conserva el estado previo, no es una segunda copia de trabajo sincronizada. La limpieza anterior eliminó cachés regenerables de npm/Go y dejó `DerivedData` de Xcode recuperable en la Papelera; no fue suficiente por sí sola para resolver las descargas pendientes.
- [x] Ajustar la configuración de `act(...)` en las pruebas de interacción de precuenta (2026-09-12). `test/precuenta-cierre.test.ts` y `test/precuenta-flujo.test.ts` activan y restauran `IS_REACT_ACT_ENVIRONMENT`; `onTestFinished` desmonta la raíz dentro de `await act(...)` y elimina el contenedor incluso si falla una aserción. No se silenció la consola ni se eliminaron comprobaciones. Verificación: ambas pruebas focalizadas y suite completa de 494 pruebas/81 archivos aprobadas sin avisos de `act` ni salida `stderr`; Go y build de producción aprobados. React y React DOM se mantienen en 19.2.8; la evaluación de 19.3.0 no implica una actualización de dependencias.

- [x] Migración del runtime de producción de Node/Hono a Go compilado: `npm start`, `npm run dev` y `npm run dev:go` ejecutan Go sobre la misma base SQLite. Cubre autenticación y roles, configuración, carta y administración de categorías/productos/recetas/contornos, inventario, salón y fondos, cuentas, órdenes y correcciones, KDS, incidencias, entrega/retiro, precuenta/caja/cancelación, red, impresión, jornadas, respaldo y reinicio demo. Incluye inicialización idempotente de instalaciones nuevas, pruebas Go por módulo, pruebas HTTP de permisos y cancelaciones, análisis estático y smoke test real del binario con una base temporal. La interfaz usa exclusivamente el contrato nuevo de cuentas y órdenes; los adaptadores históricos `/api/pedidos` quedan retirados del runtime Go. El backend Node permanece temporalmente disponible solo mediante `npm run dev:node` y `npm run start:node` como referencia de paridad, no como runtime predeterminado.

- [x] Botón "Reiniciar día de demostración" en Administración. El botón y `scripts/reiniciar-dia-demo.ts` usan el mismo endpoint administrativo; antes de reemplazar el movimiento crea un respaldo consistente, abre una jornada demo nueva y registra el evento.
- [x] Día operativo: cuentas, órdenes, comandas e incidencias quedan asociadas a una jornada; cocina y Órdenes muestran solo la abierta. Administración permite abrir y cerrar explícitamente, bloquea el cierre con trabajo pendiente y conserva resumen, responsables, horas y ruta del respaldo en la auditoría. Cerrado el 2026-09-07 en `feat/jornada-operativa`, con pruebas de migración, filtros, cierre, respaldo y reinicio, más revisión visual a 390/768/1280.
- [x] Política segura de correcciones y anulaciones implementada: permisos explícitos por rol y acción; el mesero cancela antes de preparar con PIN y motivo, Cocina cancela únicamente productos iniciados y devuelve la receta completa, y Administración o encargado de turno autorizan anulaciones totales iniciadas y pedidos para llevar confirmados. Todas las operaciones conservan historial y no usan merma ni reasignación de platos.
- [x] Bandeja de incidencias y actualizaciones del mesero implementada: acceso visible con contador y filtro, aviso contextual por orden, aceptación o rechazo de reemplazos con PIN y reconocimiento de cancelaciones ya resueltas por Cocina. Las incidencias resueltas salen de pendientes y conservan historial.
- [x] Confirmación de entrega híbrida implementada: acción manual `Entregado`, respaldo automático configurable y activado por defecto a 30 minutos, pausa ante incidencias pendientes y registro del origen manual o automático. Los pedidos para llevar se marcan `Retirado` manualmente y no usan el temporizador.
- [x] Corrección desde la precuenta implementada: mientras la mesa siga abierta, el mesero corrige con PIN, motivo rápido y detalle opcional; el total y el inventario se recalculan y la precuenta se renueva. Las cuentas cerradas permanecen inmutables.
- [ ] Completar y publicar el `Manual funcional y de operación del sistema`, usando `docs/FLUJOS_CONDICIONALES_SISTEMA_2026-09-10.md` como borrador base, para uso interno, aceptación y presentación a compradores. Debe declarar de forma exhaustiva qué puede hacer el sistema y qué no puede hacer, y cubrir cada flujo con lenguaje comprensible, actores, condición `si ocurre`, resultado, permisos, restricciones, errores y recuperación. En cada opción configurable debe indicar si se puede cambiar, quién puede hacerlo, dónde se configura y cuál es el valor predeterminado. Debe distinguir con claridad las capacidades implementadas, las aprobadas pero aún pendientes, las decisiones abiertas y las funciones fuera de alcance. La versión definitiva se produce solo después de auditar y verificar la versión entregable; debe incluir capturas reales y vigentes de teléfono, tablet y monitor, validar cada afirmación contra el sistema y mantenerse actualizada con todo cambio funcional.
- [ ] Fase 4.3: matriz formal de capturas 390/768/1280 de las 5 pantallas core, unificando las capturas que ya existen por ronda.
- [x] KDS v2 aplicado: dos columnas activas, órdenes listas fuera del tablero con contador, márgenes corregidos, una acción principal por etapa y detalle con reporte, reemplazo o cancelación de producto.
- [ ] Fase 5.3: establecer la rutina de release. Cualquier módulo de pagos requiere decisión explícita del negocio.
- [ ] Validar en el Windows del restaurante el instalador multifichero generado con `UseSetupLdr=no`: extraer el ZIP completo, ejecutar el `.exe` junto a sus `.bin` y comprobar instalación inicial, arranque automático, acceso a `127.0.0.1:8080` y ausencia del bloqueo de Seguridad de Windows sobre `%TEMP%`. El instalador anterior falló en la validación final, informó incorrectamente una restauración en primera instalación y abrió el navegador sin servidor. La corrección distingue primera instalación de actualización, normaliza la URI SQLite de Windows, guarda registros en `C:\ProgramData\Restaurante\logs`, espera la salud del servidor y añade una prueba de instalación inicial al CI Windows. Esta tarea solo se cierra con la compilación y prueba Windows en verde y la comprobación real en ese equipo.
  - Avance 2026-09-24: en la VM Windows limpia, el paquete multifichero completó la primera instalación sin error de restauración, abrió el login y `127.0.0.1:8080/api/salud` respondió `{"ok":true,"runtime":"go"}`. Se detectó que el servidor no servía `/marcas/`; la corrección y el respaldo textual del logo ya están en código y probados localmente. Queda instalar esa nueva compilación en la VM y comprobar el arranque tras reiniciar Windows antes de marcar esta tarea con `x`.
  - Avance 2026-09-25: se generó desde `codex/seguridad-local` el instalador `Restaurante-Setup-0.1.0-windows-pass2-x64.exe` y la segunda revalidación de instalación terminó correctamente según el registro. La validación interna devolvió `resultado de validación = 0` y `tarea y servidor disponibles = 1`; salud Go, HTML, logo, fuente, favicon, login por API, configuración y cierre de sesión respondieron correctamente. El desinstalador quitó el binario y detuvo el servidor, conservando la base de prueba. Aún falta reiniciar Windows y comprobar el arranque posterior; por eso la tarea sigue abierta.
  - Avance posterior 2026-09-25: Luis ejecutó una limpieza completa de los restos de instalación y la base de prueba. El paquete `0.1.0-clean-pass3` compilado desde `main` se instaló sobre ese estado vacío: Setup terminó con código 0, creó una base nueva, registró y arrancó el servicio según su log; salud, interfaz y sesión respondieron. Antes de cerrar faltan reinicio, actualización con datos y recuperación ante fallo. Los detalles y el riesgo de credenciales iniciales en red están en `docs/REVISION_INSTALACION_STACK_2026-09-25.md`.
  - Avance final 2026-09-25: dos reinicios comprobaron el arranque automático; la actualización `pass4` conservó una categoría de prueba con el mismo ID y creó un respaldo. `pass5` se canceló porque McAfee no pudo cerrarse y dejó la tarea detenida, recuperada tras reiniciar. El instalador `pass6` incorpora reanudación al cancelar y scripts de respaldo/restauración más estrictos, se instaló bien y conservó el dato. Pasaron 519 pruebas Vitest, Go y build. Sigue sin probarse una cancelación nueva con esa corrección, el rollback real provocado dentro de Inno Setup (la revisión automática bloqueó construir el paquete inválido) y el acceso desde otro equipo. No publicar aún el instalador para operación.
  - Seguimiento del salón: en el paquete instalado `pass6` se reprodujo que Mesa 1 no abre por HTTP usando la IP de Windows; la consola mostró `crypto.randomUUID is not a function`. La corrección usa `crypto.getRandomValues`; el paquete `pass7` se instaló y Mesa 1 y «Nueva orden» abrieron por IP y localhost en 8080. La cancelación causada por McAfee reanudó esta vez la tarea y conservó el paquete anterior. Luis confirmó que la captura era de otro equipo: queda instalar y validar allí. La guía de soporte identifica la tarea `Restaurante POS`, registros y cuentas iniciales.
  - Configuración y reinicio: `pass8` se actualizó sin el parámetro especial para McAfee. `pass9` se instaló con código 0, aseguró el archivo de configuración persistente y registró `Restaurante POS - Reiniciar`. Desde Opciones, el botón reinició efectivamente el servidor y el navegador volvió al salón; el marcador SQLite siguió con ID 9. Al apagar acceso por red, localhost dio 200 y la IP dio 403; al encenderlo, la IP volvió a 200. `pass10` incluyó la advertencia de Opciones y se instaló con validación 0, servidor disponible 1 y el dato preservado. Pasaron 520 pruebas Vitest, la suite Go y el build. Aún falta comprobar el otro Windows de la captura y el rollback integral del Setup; no entregar el paquete para operación hasta resolverlos.
  - Avance 2026-09-26: la restauración aislada con migración deliberadamente inválida recuperó el hash original de SQLite y `index.html`, eliminó los archivos de actualización y volvió a validar el binario. Sigue pendiente la orquestación completa dentro de Inno Setup. El equipo actual usa DHCP (`192.168.1.85`) y perfil Público. Se añadió al instalador una tarea optativa y desmarcada para una regla TCP 8080 limitada a `restaurante.exe`, `LocalSubnet` y perfil Privado; el paquete multifichero compiló con Inno 6.7.3. Falta confirmar acceso desde otro dispositivo de confianza y reservar la IP en el router del equipo objetivo.
  - Prueba real 2026-09-26: el paquete nuevo se actualizó con datos, creó respaldo, arrancó sano y conservó la categoría ID 9. Seleccionar la casilla LAN creó exactamente la regla de programa/TCP 8080/`LocalSubnet`/`Private`; se retiró después. El desinstalador quitó ejecutable y regla conservando SQLite, y la reinstalación arrancó con el dato intacto y sin regla. Mesa 1 y «Nueva orden» abrieron por localhost e IP en este equipo; Órdenes e Inventario cargaron por IP. Faltan el acceso desde otro dispositivo y la instalación en el otro Windows de la captura. La revisión automática impidió generar una actualización deliberadamente fallida para Inno: el rollback completo sigue sin validar y bloquea el merge y la entrega operativa.
  - Recorrido web adicional 2026-09-26: en localhost, inicio de sesión, Mesa 1, productos, resumen, vuelta al salón sin enviar, vistas de mesero y cocina, búsqueda de Inventario y Opciones respondieron sin errores de consola. El borrador persistió al volver, como está diseñado; la nota anterior que decía que se descartaba era incorrecta. Se cambió el botón engañoso «Cancelar» por «Volver» con aviso de persistencia. La UI nueva se revisó a 1280/390, pasaron 520 Vitest, Go y build. El paquete `pass11` se instaló con elevación, código 0, API sana y dato SQLite ID 9 conservado; mostró el nuevo texto. Los clics del navegador integrado fueron inconsistentes en otra sesión (la cinta añadió un producto y el clic sobre Inventario no navegó), mientras el teclado sí funcionó; la interacción con ratón real en Chrome/Edge sigue pendiente. El workflow de `dd7ecc0` terminó correctamente. Ver `docs/REVISION_INSTALACION_STACK_2026-09-25.md`.
  - Presentación del catálogo 2026-09-26: los identificadores internos `menu-real:...` se mostraban en Inventario y en la carta. Se ocultaron solo esos identificadores; los códigos comerciales y la búsqueda por código siguen disponibles. `pass12` se compiló e instaló con elevación, código 0, servidor sano y dato SQLite ID 9 conservado. En la copia instalada por localhost, 81 productos de la carta y 82 materiales de Inventario cargaron sin `menu-real:` visible.
  - Clics después de `pass12`: en pestañas nuevas del navegador integrado de este Windows, Inventario, Mesa 1, producto, cinta del resumen, Volver y Nueva orden respondieron por `127.0.0.1`; Mesa 1, Nueva orden e Inventario respondieron por `192.168.1.85`. El producto de prueba se retiró sin enviar pedido y Mesa 1 quedó libre. La pestaña antigua dio resultados erráticos, por lo que aún se requiere la prueba en Chrome/Edge del otro Windows y desde otro dispositivo.
  - Reinicio de `pass12`: desde Opciones se confirmó el reinicio sin cuentas en curso. El proceso del puerto 8080 cambió, los registros mostraron el nuevo arranque, `/api/salud` respondió por localhost e IP y la categoría de prueba mantuvo el ID 9.
  - Respaldo seguro y paquete para el otro Windows: los scripts ahora esperan el cierre de `restaurante.exe` antes de copiar o restaurar SQLite y verifican la salud tras restaurar. `pass13` se instaló dos veces con respaldo, salud y dato ID 9 preservado; la segunda actualización usó el script de espera ya instalado. Pasaron el rollback aislado, 520 Vitest, Go y build Windows. El ZIP recomendado, `Restaurante-Setup-0.1.0-safe-backup-pass13-x64.zip`, quedó en Documentos con Setup, ambos `.bin`, instrucciones y SHA-256 verificados. Falta instalarlo y recorrer los botones en el otro equipo; la recuperación integral ante fallo de Inno sigue sin validar.
  - Alcance acordado 2026-09-26: Luis cerró esta revisión sin acceso al otro Windows y pidió probar en este equipo. El Windows actual es Home 24H2 y no dispone de Windows Sandbox. El respaldo real `20260926-015237` pasó `PRAGMA integrity_check` y conservó la categoría de control ID 9. Los clics por localhost e IP se probaron aquí; el otro equipo y el acceso desde otro dispositivo quedan fuera de esta validación local, registrados como límites. La recuperación integral ante fallo de Inno sigue sin validar porque la revisión automática bloqueó la preparación de un Setup fallido. La documentación de Inno confirma que el paso de restauración ocurre después de finalizar el registro de instalación, cuando sus errores ya no disparan una reversión automática; la restauración aislada pasó. No se identificó un fallo conocido que bloquee integrar el código, pero el instalador **no se distribuye para uso operativo** hasta verificar el caso completo.
  - Seguimiento tras el merge de la PR #20: también se intentó restaurar una copia del respaldo real en una carpeta temporal con el script instalado. La revisión automática rechazó el comando antes de ejecutarlo (`blocked by policy`); no se modificó la instalación. La restauración de ese respaldo y el fallo integral de Inno permanecen pendientes antes de distribuir el instalador.
  - Estado 2026-09-26: los criterios observados y los pendientes, con el procedimiento que requerirá colaboración manual en un equipo de prueba, están separados en `docs/PRUEBAS_PENDIENTES_WINDOWS.md`. El rechazo ocurrió antes de arrancar PowerShell aun con la configuración local de aprobación `never` y acceso completo; no se identificó una regla de Windows ni se ejecutó la prueba integral. Mantener pendiente la distribución del instalador.
  - Avance posterior 2026-09-26 en este mismo Windows: Luis confirmó que los productos son reales y el resto de los datos de la instalación son de prueba. Se copiaron y compararon por SHA-256 los 83 archivos del respaldo previo fuera de `ProgramData`; una instantánea consistente de la base activa pasó `integrity_check` y conservó el dato ID 9. Un Setup de prueba copió la interfaz y una migración nuevas, ejecutó la validación y forzó un fallo: Inno restauró la versión y SQLite, pero devolvió erróneamente código 0. Se corrigió la salida a 20 (21 si falla también la restauración) y se incluyó la escritura de `install-success.marker` en el mismo camino de recuperación. Repetir el fallo tras validar y otro después de arrancar la nueva versión devolvió 20; en ambos casos el ejecutable/HTML anterior, la salud y el hash de SQLite quedaron intactos y desapareció la migración de prueba. La actualización normal `pass15` devolvió 0, validación 0, servidor disponible 1 y preservó el hash de SQLite. Falta reiniciar Windows tras `pass15` y cerrar las demás comprobaciones locales antes de fusionar la PR nueva. Detalles y registros en `docs/PRUEBAS_PENDIENTES_WINDOWS.md`.
- [x] Fase 5.4: endurecimiento ligero para la red local. Cinco PIN incorrectos desde el mismo equipo activan una pausa de 60 segundos, sin bloqueo permanente y con reinicio del contador al acertar; las sesiones tienen vencimiento absoluto de 16 horas comprobado por Go, sin renovarse por sondeos automáticos. Administración puede configurar 3–10 intentos, 15–300 segundos y 4–24 horas desde Opciones. Los límites viven en memoria y se limpian al reiniciar el servidor, decisión intencional para no interrumpir la operación local. Verificado con pruebas unitarias, HTTP, suite React, suite Go, build de producción y revisión visual móvil.

## Cómo se trabaja (vigente desde el 2026-09-06)

- `main` es la rama por defecto; el trabajo se integra por PR a `main`.
- Una rama por tarea creada desde `main`; al cerrar, `npm test` y `npm run build` en verde.
- Cambios de interfaz: verificación responsive en monitor, tablet y smartphone (390/768/1280) con capturas en `capturas/<fecha>_<tema>/` y su `manifest.json`.
