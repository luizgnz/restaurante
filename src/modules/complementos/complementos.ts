export function listarPlugins(): [] {
  return [];
}

export function mensajesVacios(): ["No hay plugins para mostrar", "No hay plugins disponibles"] {
  return ["No hay plugins para mostrar", "No hay plugins disponibles"];
}
